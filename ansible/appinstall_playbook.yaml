---
# Recuerden: Se pueden enviar la ejecucion de modulos hacia todos los grupos de servidores definidos en el inventario.
- hosts: cluster_servers, data_servers
  become: true
  roles:
    - { role: common-app, tags: ["commonapp"] }
